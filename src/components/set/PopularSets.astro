---
import { PokeSetHandler } from "../../handlers/PokeSetHandler";
import SetGrid from "./SetGrid.astro";

const { lang } = Astro.locals;
const language = lang.toString() || "en";

const { sets, error } = await PokeSetHandler.getAll({ language });

sets?.reverse();
---

<SetGrid sets={sets} />
